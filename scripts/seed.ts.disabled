// NOTE: seed.ts is a development helper — imports reference legacy paths
// that may not exist at runtime in v4+. Update imports if re-enabling.
// The file body below is disabled until imports are restored.
export {};
