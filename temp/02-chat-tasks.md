# AI Chats — 100 Improvements & Fixes

> Queue: `#chat` | Priority: P0-P3 | Module: AI Chats

---

## 🔴 Критические Баги (P0) — #101–#110

| # | Приоритет | Описание | Файл | Статус |
|---|-----------|----------|------|--------|
| 101 | P0 | **admin-service.ts эмитит `'SEND_MESSAGE'` (строку), а не `EVENTS.SEND_MESSAGE` (`'chat:send'`)** — ивент мёртвый, никто не ловит | `src/kernel/services/admin-service.ts:238` | ❌ Open |
| 102 | P0 | **chat-service.ts эмитит стриминговые ивенты как raw strings `'chat:stream:*'` вместо `EVENTS.STREAM_*`** — код-стайл, работает (строки совпадают), но ломает типизацию | `src/kernel/services/chat-service.ts:178-250` | ❌ Open |
| 103 | P0 | **429 рекурсия в executeRequest без depth limit** — если все провайдеры в fallback chain тоже 429, будет бесконечная рекурсия | `src/kernel/services/chat-service.ts:299-315` | ❌ Open |
| 104 | P0 | **`resolveWithFallback` может вернуть того же провайдера** — fallback chain выбирает другой ключ того же провайдера, который только что дал 429 | `src/kernel/services/chat-service.ts:305` | ❌ Open |
| 105 | P0 | **requestId matching через `startsWith`** — `requestId.startsWith(entry.requestId!)` может совпасть с чужим requestId при коллизии префиксов | `src/stores/useChatStore.ts:139,169,200,214,255` | ❌ Open |
| 106 | P0 | **requestId = `crypto.randomUUID().slice(0,8)` — 8 hex символов** — ~4 млрд комбинаций, но коллизия возможна под нагрузкой | `src/stores/useChatStore.ts` | ❌ Open |
| 107 | P0 | **`isStreamingRef` никогда не устанавливается в `true`** — в ChatPanel строки 301-302 проверяют `isStreamingRef.current`, но он всегда `false`, авто-скролл никогда не корректно блокируется | `src/components/ChatPanel/ChatPanel.tsx:301` | ❌ Open |
| 108 | P0 | **`h.responses.filter(r => r.status === 'done').slice(0,1)`** — в истории сохраняется только первый ответ из нескольких провайдеров, остальные теряются при ре-рендере | `src/stores/useChatStore.ts:327` | ❌ Open |
| 109 | P0 | **ChatService не экспортируется из `instances.ts`** — нет DI-синглтона, хотя в bootstrap.ts он регистрируется | `src/kernel/instances.ts` | ❌ Open |
| 110 | P0 | **`console.log`/`console.warn`/`console.error` в ChatService вместо ILogger** — нарушает observability contract | `src/kernel/services/chat-service.ts` | ❌ Open |

---

## 🟠 Логические проблемы высокой важности (P1) — #111–#125

| # | Приоритет | Описание | Статус |
|---|-----------|----------|--------|
| 111 | P1 | **Нет индикатора "Thinking..."** — при медленном старте стриминга (TTFT > 3-5с) пользователь видит пустой экран без обратной связи | ❌ Open |
| 112 | P1 | **Multi-provider параллельная отправка коллапсится в один ответ** — `filter.slice(0,1)` теряет все response от второго, третьего провайдера | ❌ Open |
| 113 | P1 | **MemoryService.store() fire-and-forget** — на строке 238-249 useChatStore вызывает `memoryService.store()` без await, ошибки тихо проглатываются | ❌ Open |
| 114 | P1 | **MemoryService.search() блокирует отправку** — `await memoryService.search(text, 3)` может упасть и заблокировать весь send | ❌ Open |
| 115 | P1 | **`handleRegenerate` не учитывает выбранную модель** — регенерирует с той же моделью, даже если пользователь сменил модель после отправки | ❌ Open |
| 116 | P1 | **Split-сравнение работает только с первыми 2 ключами** — если выбрано >2 ключей, остальные игнорируются | ❌ Open |
| 117 | P1 | **При смене активной сессии во время стриминга — race condition** — чанки могут дописаться в старую сессию | ❌ Open |
| 118 | P1 | **Быстрая двойная отправка ломает `currentRequestIdRef`** — второй send перезаписывает ref до завершения первого | ❌ Open |
| 119 | P1 | **handleSend() использует `historyRef.current` (snapshot на момент отправки), но `updateActiveSession` может мутировать сессию** | ❌ Open |
| 120 | P1 | **`modulesInfo` всегда рендерится** — `<ModuleInfo moduleKey="chat" />` на строке 736 не имеет loading/empty состояния | ❌ Open |
| 121 | P1 | **Авто-роутинг `provider === 'auto'` — case-sensitive** — `'auto'` сработает, `'Auto'` или `'AUTO'` — нет | `chat-service.ts:100` | ❌ Open |
| 122 | P1 | **selectFromPool может не найти ключ — `undefined` без обработки** | ❌ Open |
| 123 | P1 | **Cache hit path эмитит stream:* ивенты с захардкоженной latency=50ms** — не учитывает реальное время кэша | ❌ Open |
| 124 | P1 | **Нет дедупликации RAG запросов** — при parallel mode каждый target делает `memoryService.search()` независимо | ❌ Open |
| 125 | P1 | **Данные в localStorage не синхронизируются при закрытии вкладки** — последние изменения теряются | ❌ Open |

---

## 🟡 Средние логические / архитектурные (P2) — #126–#140

| # | Приоритет | Описание | Статус |
|---|-----------|----------|--------|
| 126 | P2 | **Нет сообщения "провайдер печатает" при медленном TTFT** | ❌ Open |
| 127 | P2 | **MessageSearch не включён — поиск только по заголовкам сессий** | ❌ Open |
| 128 | P2 | **Нет auto-scroll lock** — при просмотре истории новое сообщение всегда скроллит вниз | ❌ Open |
| 129 | P2 | **Нет undo при редактировании сообщения** — старый текст безвозвратно теряется | ❌ Open |
| 130 | P2 | **Нет export/import для одной сессии** — только bulk через ChatAdminPanel | ❌ Open |
| 131 | P2 | **Нет визуализации fork-дерева** — кто от кого ответвился не видно | ❌ Open |
| 132 | P3 | **Временные метки только на уровне сессии, не на уровне сообщения** | ❌ Open |
| 133 | P3 | **Нет кнопки "scroll to bottom"** — авто-скролл всегда включён | ❌ Open |
| 134 | P2 | **Quick-reply кнопки отправляют мгновенно** — нет подтверждения, 100ms timeout | ❌ Open |
| 135 | P2 | **Empty-state предложения статичны** — не адаптируются под текущий набор провайдеров | ❌ Open |
| 136 | P3 | **Режимы Auto/Parallel/Single — маленький select, непонятно** | ❌ Open |
| 137 | P3 | **Нет подсказки Enter → отправить, Shift+Enter → новая строка** | ❌ Open |
| 138 | P3 | **Нет Markdown-подсказки в поле ввода** — "Поддерживается Markdown" | ❌ Open |
| 139 | P3 | **Нет отмены отправки через Shift+клик на Send** — только через Stop/X кнопку | ❌ Open |
| 140 | P2 | **Debounce (1с) при синке с Dexie может потерять данные при быстром закрытии** | ❌ Open |

---

## 🎨 UI/UX Улучшения (P2-P3) — #141–#165

| # | Приоритет | Описание | Статус |
|---|-----------|----------|--------|
| 141 | P2 | **🏁 Provider Key Health Check панель** — кнопки для каждого активного ключа: "Test Key" с авто-подбором модели | ❌ Open |
| 142 | P1 | **Bulk-test: кнопка "Test All Keys"** — отправляет тестовый промпт на все активные ключи параллельно | ❌ Open |
| 143 | P2 | **Результат теста: таблица "ключ → модель → статус → latency → ответ"** | ❌ Open |
| 144 | P2 | **Зелёная галочка / красный крест напротив каждого ключа после теста** | ❌ Open |
| 145 | P3 | **Авто-подбор модели для теста** — выбирает самую быструю/дешёвую модель у каждого провайдера | ❌ Open |
| 146 | P2 | **Индикатор "готов к диалогу" после успешного теста** | ❌ Open |
| 147 | P3 | **Прогресс-бар для bulk-test** — "Проверено 3/7 ключей" | ❌ Open |
| 148 | P3 | **История тестов** — логи прошлых проверок с результатами | ❌ Open |
| 149 | P3 | **Групповой тест с выборкой (test only selected)** — чекбоксы "тестировать этот ключ" | ❌ Open |
| 150 | P3 | **"Повторить тест" — кнопка ретеста для упавших ключей** | ❌ Open |
| 151 | P2 | **Виджет "Health Status" после теста** — все зелёные = "System OK", есть красные = "N keys need attention" | ❌ Open |
| 152 | P3 | **Контекстное меню на ключе: → Test → Send Test Message** | ❌ Open |
| 153 | P2 | **Автоматический тест после добавления нового ключа** | ❌ Open |
| 154 | P3 | **Выбор тестового промпта** — "Hello" / "What is AI?" / кастомный | ❌ Open |
| 155 | P3 | **Сортировка ключей по результатам теста (рабочие сверху)** | ❌ Open |
| 156 | P3 | **Side-by-side ответов в split-view** — сейчас 2 колонки, можно расширить до N | ❌ Open |
| 157 | P3 | **Добавить кнопку "Copy response as markdown"** | ❌ Open |
| 158 | P3 | **Добавить export чата как HTML/PDF/TXT** | ❌ Open |
| 159 | P3 | **Горячие клавиши: Ctrl+Enter → отправить, Esc → cancel, Ctrl+Shift+F → поиск по сообщениям** | ❌ Open |
| 160 | P3 | **Добавить кастомный system prompt в UI чата** | ❌ Open |
| 161 | P3 | **Слайдер температуры (temperature) в панели отправки** | ❌ Open |
| 162 | P3 | **Выбор maxTokens в UI** | ❌ Open |
| 163 | P3 | **Добавить drag-and-drop файлов в чат** | ❌ Open |
| 164 | P3 | **Изображения в Markdown-рендере** — поддержка `![alt](url)` | ❌ Open |
| 165 | P2 | **Синтаксис-хайлайтинг кода в MarkdownRenderer** | ❌ Open |
| 166 | P3 | **Таблицы в MarkdownRenderer** — structured `<table>` вместо моноширинного текста | ❌ Open |
| 167 | P3 | **Голосовой ввод сообщений** | ❌ Open |
| 168 | P3 | **Выбор языка ответа** | ❌ Open |
| 169 | P3 | **Закреплённые сообщения (pin)** | ❌ Open |
| 170 | P3 | **Закладки на сообщения (bookmark)** | ❌ Open |
| 171 | P3 | **Кнопка "Поделиться чатом"** | ❌ Open |
| 172 | P3 | **Кастомные цвета/иконки для сессий** | ❌ Open |
| 173 | P3 | **Шаблоны чатов** — "Technical Support", "Code Review", "Creative Writing" | ❌ Open |
| 174 | P3 | **Превью ссылок в сообщениях** | ❌ Open |
| 175 | P2 | **Умный ввод** — `@provider:model` в тексте меняет таргет для этого сообщения | ❌ Open |
| 176 | P3 | **Кнопка "Retry on different provider" при ошибке** | ❌ Open |
| 177 | P3 | **Экспорт только ответов (без промптов)** | ❌ Open |
| 178 | P2 | **Skeleton loading вместо bouncing dots** | ❌ Open |
| 179 | P3 | **Показать токены ответа в реальном времени при стриминге** | ❌ Open |
| 180 | P3 | **Добавить "scroll to top" FAB кнопку** | ❌ Open |
| 181 | P3 | **Кэш последнего промпта** — Ctrl+Shift+Z восстанавливает последнее отправленное сообщение | ❌ Open |
| 182 | P3 | **Перенос режимов (auto/parallel/single) в настройки, не в select** | ❌ Open |
| 183 | P3 | **Reactions к сообщениям (как в Slack/Teams)** | ❌ Open |
| 184 | P3 | **Ответ с цитированием (reply-to)** | ❌ Open |
| 185 | P3 | **Выбор тестового промпта из preset-ов** | ❌ Open |

---

## ⚡ Производительность (P2) — #186–#190

| # | Приоритет | Описание | Статус |
|---|-----------|----------|--------|
| 186 | P2 | **Виртуализация списка сообщений** — с 100+ сообщениями начинаются лаги | ❌ Open |
| 187 | P2 | **`useKeyStore` ре-рендерит ChatPanel при любом изменении ключей** — нет селекторов | ❌ Open |
| 188 | P2 | **session persistence загружает ВСЕ сессии из Dexie на mount** — нет lazy loading | ❌ Open |
| 189 | P3 | **Нет дебаунса на searchQuery в ChatAdminPanel** | ❌ Open |
| 190 | P2 | **`getAllMessages()` не используется — дублирование данных** — история хранится в `history[]` внутри сессии И в `chatMessages` таблице | ❌ Open |

---

## 🔒 Безопасность (P2) — #191–#195

| # | Приоритет | Описание | Статус |
|---|-----------|----------|--------|
| 191 | P2 | **Контент сообщений не санируется** — XSS возможен через MarkdownRenderer (хотя HTML entities экранируются) | ❌ Open |
| 192 | P1 | **Логи чатов содержат API ключи в error-сообщениях от провайдеров** | ❌ Open |
| 193 | P3 | **Нет auto-delete для старых сессий** — неограниченный рост IndexedDB | ❌ Open |
| 194 | P2 | **Dexie validation hooks могут блокировать запись старых сессий** | ❌ Open |
| 195 | P3 | **Export сессий содержит весь промпт-историю** — нет режима "без sensitive data" | ❌ Open |

---

## 🧪 Тесты (P1) — #196–#200

| # | Приоритет | Описание | Статус |
|---|-----------|----------|--------|
| 196 | P1 | **Нет тестов на streaming paths** — chunk accumulation, start/end/error | ❌ Open |
| 197 | P1 | **Нет тестов на session CRUD** — create, delete, fork, rename | ❌ Open |
| 198 | P2 | **Нет тестов на handleSend / handleRegenerate** | ❌ Open |
| 199 | P2 | **Нет тестов на MarkdownRenderer** — code blocks, headers, lists | ❌ Open |
| 200 | P1 | **Нет E2E тестов на полный флоу: add key → open chat → send message → receive response** | ❌ Open |

---

## Сводка

| Категория | Кол-во | P0 | P1 | P2 | P3 |
|-----------|--------|:--:|:--:|:--:|:--:|
| 🔴 Critical Bugs | 10 | 10 | — | — | — |
| 🟠 High Priority Logic | 15 | — | 15 | — | — |
| 🟡 Medium Logic/Arch | 15 | — | — | 12 | 3 |
| 🎨 UI/UX | 45 | — | 1 | 10 | 34 |
| ⚡ Performance | 5 | — | — | 4 | 1 |
| 🔒 Security | 5 | — | 1 | 3 | 1 |
| 🧪 Testing | 5 | — | 3 | 2 | — |
| **Total** | **100** | **10** | **20** | **31** | **39** |
