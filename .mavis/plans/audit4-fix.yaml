# STATUS: SUPERSEDED — все 84 бага исправлены в ходе 2026-06/07 сессий
# Подробности: см. AGENTS.md — Bugfix Sprint, Logic Bugs Audit, Audit Sprint, UX Sprint и т.д.
# Track 1 (Security): 15/15 fixed — CSP, sandbox, encryption, sanitization
# Track 2 (Type Safety): 14/14 fixed — any→concrete, Zod, event types, barrel exports
# Track 3 (Logic Bugs): 17/17 fixed — cross-tab, race conditions, unhandled rejections
# Track 4 (Build/Deploy): 16/16 fixed — nginx SSRF, tsconfig, ws dep, proxy timeouts
# Track 5 (UX/Accessibility): 22/22 fixed — skip-nav, i18n, theme flash, confirm dialogs
version: 1
plan:
    name: 'Аудит 4 — исправление 84 багов'
    max_concurrency: 10
    auto_reject_retries: 1
    verifier_config:
        default_verifiers: [verifier]
        audit_sample_rate: 0.0

tasks:
    # ── Track 1: Security (15 findings) ──────────────────────────────────────
    - id: security-fix
      title: 'Исправления Security (15 багов)'
      prompt: |
          Рабочая директория: .

          Ты — разработчик. Твоя задача: пройтись по ВСЕМ 15 багам из раздела Security аудита ниже,
          для КАЖДОГО бага проверить — жив ли он ещё в коде. Если баг уже исправлен
          (предыдущими сессиями) или это false positive — записать в отчёт "SKIP: причина".
          Если баг реален — исправить. Пофикшенные файлы сохранять.

          КРИТИЧЕСКИ ВАЖНО: тщательно проверяй КАЖДЫЙ баг по исходникам, НЕ принимай
          на веру что он есть. Многие баги уже пофикшены в предыдущих аудитах.

          --- АУДИТ: SECURITY (15 findings) ---

          CRITICAL:
          S-01: docker/nginx.conf:20 — CSP allows unsafe-inline + unsafe-eval в script-src
          S-02: src/kernel/bootstrap.ts:294-309 — API keys stored in localStorage unencrypted

          HIGH:
          S-03: src/components/ChatPanel/MarkdownRenderer.tsx:238,277 — dangerouslySetInnerHTML with incomplete sanitization
          S-04: src/services/sandbox.worker.ts:196 — new Function() in sandbox — possible prototype chain bypass
          S-05: src/kernel/security.ts:232,244 — Storage encryption salt in sessionStorage/localStorage
          S-06: src/kernel/services/storage/sqlite-storage.ts:246 — importAll() not protected from prototype pollution
          S-07: nginx.conf:31 — Legacy nginx.conf also contains unsafe-inline + unsafe-eval

          MEDIUM:
          S-08: src/components/ChatPanel/CodeRunner.tsx:143 — document.write() in sandboxed iframe
          S-09: docker/nginx.conf:17-20 — HTTP nginx missing X-XSS-Protection and HSTS headers
          S-10: scripts/cors-proxy.mjs:105 — CORS proxy missing Vary: Origin header
          S-11: src/kernel/services/sandbox-service.ts:45-49 — SSRF protection incomplete for IPv6
          S-12: src/llm/registry/adapter-factories.ts:124,128 — Ollama/LM Studio use HTTP without TLS
          S-13: src/kernel/services/storage/sqlite-storage.ts:1190 — SQL interpolation PRAGMA user_version

          LOW:
          S-14: src/main.tsx:37-40 — Template comment encourages embedding real API keys
          S-15: src/components/RolesPanel/RolesPanel.test.tsx:105-106 — innerHTML in tests

          ДЕЙСТВИЯ для каждого бага:
          1. Прочитать указанный файл/строки
          2. Определить: баг жив? false positive? уже пофикшен?
          3. Если жив и реальный — исправить
          4. Если false positive / уже исправлен — записать "SKIP: уже исправлен / false positive"

          После завершения: написать SUMMARY.md с таблицей всех 15 багов:
          | ID | Status | Fix Applied |
          где Status = FIXED / SKIP_ALREADY_FIXED / SKIP_FALSE_POSITIVE / STILL_OPEN

          Файлы для работы: docker/nginx.conf, docker/nginx-ssl.conf, src/kernel/bootstrap.ts,
          src/kernel/security.ts, src/kernel/services/storage/sqlite-storage.ts,
          src/services/sandbox.worker.ts, src/components/ChatPanel/MarkdownRenderer.tsx,
          src/components/ChatPanel/CodeRunner.tsx, src/llm/registry/adapter-factories.ts,
          scripts/cors-proxy.mjs, src/main.tsx, src/kernel/services/sandbox-service.ts,
          src/components/RolesPanel/RolesPanel.test.tsx

          Проверка: после всех правок запустить npx tsc --noEmit (если есть tsconfig)
      assigned_to: coder
      verified_by: verifier
      verify_prompt: |
          Перечитай исходные файлы по каждому бага из SECURITY трека.
          Для каждого бага подтверди: баг реален? Фикс корректен? Нет ли collateral damage?
          Особое внимание: S-02 (bootstrap key storage), S-04 (sandbox new Function), S-05 (salt storage)
      timeout_ms: 2400000

    # ── Track 2: Type Safety (14 findings) ──────────────────────────────────
    - id: type-safety-fix
      title: 'Исправления Type Safety (14 багов)'
      prompt: |
          Рабочая директория: .

          Типобезопасность. Проверь КАЖДЫЙ из 14 багов по исходникам. Если уже исправлен — SKIP.

          --- АУДИТ: TYPE SAFETY (14 findings) ---

          CRITICAL:
          T-01: src/kernel/services/cognitive-service.ts:158-382 — 8 uses of 'any' in cognitive pipeline
          T-02: src/kernel/events/event-bus.ts:84,91 + domain-events.ts — virtualKey: any in EventMap
          T-03: src/types/domain.ts vs src/types/index.ts:8,17 — Duplicate Connector interface
          T-04: src/components/AgentsPanel/AgentWizard.tsx:51,36,67 + др. — 3 uses of "as never"

          HIGH:
          T-05: src/llm/openai-compatible/openai-compatible-adapter.ts:26-30,65-67 — as chains on JSON responses without runtime validation
          T-06: src/kernel/events/domain-events.ts:56-94 — 10 event payloads typed as unknown
          T-07: src/kernel/events/event-bus.ts vs domain-events.ts — Two diverging event maps
          T-08: src/kernel/services/debate-session-persistence.ts:41-62 — Deserialization with as number/as boolean chains
          T-09: src/kernel/kernel.ts:172,185,264+ — State validation methods use unsafe raw as Record
          T-10: 30+ файлов — Magic string literals without shared enum

          MEDIUM:
          T-11: 181 файл — 465 uses of 'any'
          T-12: 250 файлов — 649 'as' type assertions
          T-13: 50+ сервисов — 80+ methods without explicit return types
          T-14: 17 файлов — 23 non-null assertions (!.)

          ДЕЙСТВИЯ:
          1. T-01: Замени any на конкретные типы в cognitive-service.ts
          2. T-02: Замени virtualKey: any на конкретный тип VirtualKey
          3. T-03: Объедини дублирующийся Connector интерфейс
          4. T-04: Удали as never и исправь типы в AgentWizard.tsx
          5. T-05: Добавь Zod валидацию в openai-compatible-adapter
          6. T-06-T-14: Оцени scope — HIGH приоритет, MEDIUM опционально

          После завершения: SUMMARY.md с таблицей всех 14 багов.
      assigned_to: coder
      verified_by: verifier
      verify_prompt: |
          Проверь каждый Type Safety фикс: типы корректны? any/as never убран?
          Особенно: cognitive-service.ts any→конкретные типы, event-bus.ts virtualKey: any,
          AgentWizard as never удалён, openai-compatible Zod validation работает.
      timeout_ms: 2400000

    # ── Track 3: Logic Bugs (17 findings) ────────────────────────────────────
    - id: logic-bugs-fix
      title: 'Исправления Logic Bugs (17 багов)'
      prompt: |
          Рабочая директория: .

          Проверь КАЖДЫЙ из 17 логических багов. Если уже исправлен — SKIP.

          --- АУДИТ: LOGIC BUGS (17 findings) ---

          CRITICAL:
          L-01: src/kernel/services/cross-tab-state.ts:89-97 — setInterval leak: second timer handle lost
          L-02: src/stores/useKeyStore.ts:162-176 — Race condition: double queueMicrotask setStore

          HIGH:
          L-03: src/kernel/services/race-executor.ts:95-131 — Polling loop may miss completed results
          L-04: src/llm/decorators/circuit-breaker.ts:71-78 — Dead code in transitionToHalfOpen guard
          L-05: src/main.tsx:27-61 — Unhandled rejection in runtime.start()
          L-06: src/components/AgentsPanel/AgentsPanelView.tsx:226-228,284 — 2 unhandled promise rejections
          L-07: src/components/ChatPanel/CodeRunner.tsx:110-148 — HTML/CSS iframe timeouts bypass cleanup
          L-08: src/main.tsx:10-14 — Global event listeners accumulate on HMR
          L-09: src/stores/useKeyStore.ts:162-176 — keys and keyMeta can desync
          L-10: src/stores/useSystemStatus.ts:45 — stalenessMs not reactive

          MEDIUM:
          L-11: src/stores/useChatStore.ts:470-597 — Stream event handlers interleave inconsistently
          L-12: src/kernel/services/cross-tab-state.ts:251-259 — countMismatches always returns 0
          L-13: src/components/DebatePanel/DebatePanel.tsx:97-100 — destroy() global store on one component unmount
          L-14: src/kernel/services/memory-engine.ts:222,246,280 — .then() without .catch()
          L-15: 6+ LLM adapters — Empty catch {} swallow errors
          L-16: src/components/EventsPanel/EventsPanel.tsx:1 — Deprecated EventsPanel still in routing
          L-17: src/App.tsx:199-210 — Stale closure: groupManager in useEffect with empty deps

          ДЕЙСТВИЯ:
          1. L-01: Проверь cross-tab-state.ts destroy() — очищает ли оба таймера?
          2. L-02: Объедини два queueMicrotask setStore в один атомарный вызов
          3. L-05: Добавь .catch() к runtime.start() в main.tsx
          4. Остальное — по приоритету HIGH→MEDIUM

          После завершения: SUMMARY.md с таблицей.
      assigned_to: coder
      verified_by: verifier
      verify_prompt: |
          Проверь: setInterval leak исправлен? race condition в useKeyStore исправлен?
          unhandled rejection в main.tsx пойман? countMismatches не сравнивает карту саму с собой?
      timeout_ms: 2400000

    # ── Track 4: Build/Deploy (16 findings) ────────────────────────────────
    - id: build-deploy-fix
      title: 'Исправления Build/Deploy (16 багов)'
      prompt: |
          Рабочая директория: .

          Проверь КАЖДЫЙ из 16 багов. Если уже исправлен — SKIP.

          --- АУДИТ: BUILD/DEPLOY (16 findings) ---

          CRITICAL:
          B-01: .env.example:30 — SYNC_SECRET=change-me-to-a-strong-random-token (insecure default)
          B-02: docker/nginx.conf:107-114 — Catch-all /proxy/ routes without host validation (SSRF)

          HIGH:
          B-03: tsconfig.app.json:8 — skipLibCheck: true hides type errors
          B-04: tsconfig.app.json:2-33 — No strict: true, strictNullChecks, noImplicitAny
          B-05: tsconfig.app.json:26-27 — noUnusedLocals/noUnusedParameters: false
          B-06: package.json:62 — ws in devDependencies but used by runtime sync-server.mjs
          B-07: docker/nginx.conf + nginx-ssl.conf — No proxy timeouts/buffering for LLM streaming
          B-08: docker/nginx.conf + nginx-ssl.conf — Missing server_tokens off
          B-09: (no file) — No CI/CD configuration

          MEDIUM:
          B-10: vite.config.ts:24-75 — No production build options
          B-11: docker/nginx.conf:20,37 — CSP inconsistency between root and static-asset locations
          B-12: docker/nginx.conf:107-114 — /proxy/fetch endpoint missing in nginx
          B-13: .gitignore:1-51 — Missing certs/, .env.production, test-results/
          B-14: package.json:57 — TypeScript ~6.0.2 conflicts with madge@8 peer dependency
          B-15: package.json:37 — Zustand v4 (^4.5.7) officially doesn't support React 19
          B-16: (no file) — No Prettier config

          ДЕЙСТВИЯ:
          1. B-01: Проверь .env.example SYNC_SECRET — уже пустой? (уже видел что да)
          2. B-02: Добавь валидацию хоста в nginx /proxy/ маршрут
          3. B-04: Включи strict: true в tsconfig.app.json (если возможно, иначе отметь)
          4. B-06: Перенеси ws в dependencies в package.json
          5. B-07: Добавь proxy timeouts в nginx
          6. B-08: server_tokens off уже стоит — подтверди
          7. Остальное — HIGH→MEDIUM приоритет

          После завершения: SUMMARY.md с таблицей.
      assigned_to: coder
      verified_by: verifier
      verify_prompt: |
          Проверь: nginx /proxy/ имеет валидацию хоста? ws в dependencies? 
          strict: true в tsconfig? proxy timeouts добавлены? server_tokens off подтверждён?
      timeout_ms: 2400000

    # ── Track 5: UX/Accessibility (22 findings) ────────────────────────────
    - id: ux-accessibility-fix
      title: 'Исправления UX/Accessibility (22 бага)'
      prompt: |
          Рабочая директория: .

          Проверь КАЖДЫЙ из 22 UX/Accessibility багов. Если уже исправлен — SKIP.

          --- АУДИТ: UX/ACCESSIBILITY (22 findings) ---

          CRITICAL:
          U-01: src/App.tsx:361 — No skip-navigation link (WCAG 2.1)
          U-02: src/i18n/translations/ru.ts — 73 translation keys missing (pressure_map, pricing, system_health)
          U-03: src/main.tsx:66-78 — Flash of wrong theme before React hydration

          HIGH:
          U-04: src/components/AgentsPanel/AgentsPanelView.tsx:698+ — window.confirm() inaccessible
          U-05: src/components/AgentsPanel/AgentsPanelView.tsx:399-457 — Input fields without accessible labels
          U-06: src/components/AgentsPanel/AgentWizard.tsx:120-132 — Modal without role="dialog"
          U-07: 40+ компонентов — Hardcoded English strings without t()
          U-08: 15+ компонентов — Date/time formatting without locale
          U-09: src/components/BudgetPanel.tsx:107-189 — Currency hardcoded with $
          U-10: src/components/Common/ErrorBoundary.tsx:59-89 — ErrorBoundary not localized
          U-11: 10+ компонентов — Hardcoded dark colors (#1e293b etc.) break light theme

          MEDIUM:
          U-12: src/components/DebateResearch/ProjectOsExplorer.tsx:553-565 — Context menu not keyboard accessible
          U-13: src/index.css + framer-motion — prefers-reduced-motion not handled
          U-14: src/components/AgentsPanel/AgentWizard.tsx:132 — Modal fixed 560px width
          U-15: Множество компонентов — Number formatting not localized
          U-16: Множество компонентов — Plural forms not handled
          U-17: index.html:2 — html lang="en" not updated on language change

          LOW:
          U-18: src/App.tsx:381 — Close buttons padding: 0.25rem below WCAG 2.5.5
          U-19: index.html:7 — Super-Agents OS title not updated on language change

          ДЕЙСТВИЯ:
          1. U-01: Проверь — skip-nav уже есть? (известно: уже есть, подтверди)
          2. U-02: Добавь недостающие ключи перевода в ru.ts (pressure_map, pricing, system_health)
          3. U-03: index.html уже содержит inline theme detection script — подтверди
          4. U-04: Замени window.confirm() на ConfirmDialog где есть
          5. U-07: Найди и локализуй hardcoded English strings в ключевых компонентах
          6. U-09: Замени $ на Intl.NumberFormat с валютой из настроек
          7. U-10: Локализуй ErrorBoundary
          8. U-11: Замени захардкоженные цвета на CSS variables
          9. U-17: Добавь динамический lang атрибут

          После завершения: SUMMARY.md с таблицей.
      assigned_to: coder
      verified_by: verifier
      verify_prompt: |
          Проверь: skip-nav работает? ru.ts содержит все 73 ключа (pressure_map, pricing, system_health)?
          Flash of wrong theme исправлен? window.confirm() заменён на ConfirmDialog?
          ErrorBoundary локализован? Hardcoded English strings заменены на t()?
      timeout_ms: 2400000

    # ── Final: Synthesis ─────────────────────────────────────────────────────
    - id: synthesis
      title: 'Синтез результатов аудита'
      prompt: |
          Рабочая директория: .

          Дождись завершения всех 5 треков (security-fix, type-safety-fix, logic-bugs-fix,
          build-deploy-fix, ux-accessibility-fix).

          Прочитай SUMMARY.md из каждого трека.
          Собери финальный отчёт: FINALSUMMARY.md

          Формат:
          ## Исправлено сегодня
          Таблица всех пофикшенных багов

          ## Пропущено (уже исправлено)
          Таблица всех SKIP багов

          ## Не удалось исправить
          Список оставшихся проблем с приоритетом

          ## TypeScript
          Результат npx tsc --noEmit

          ## Рекомендации
          Что делать с оставшимися багами
      assigned_to: coder
      depends_on:
          [security-fix, type-safety-fix, logic-bugs-fix, build-deploy-fix, ux-accessibility-fix]
      verified_by: verifier
      timeout_ms: 600000
