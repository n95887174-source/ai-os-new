# AI Agent Context — SuperAgents OS v3.7.1
> **The Observable and Programmable Intelligence Operating System**

This project is an **autonomous, event-driven, decision-centric multi-agent runtime**. It shifts the focus from simple LLM chat to structured cognitive execution.

**Current testing baseline: 32 test files, 192 tests, all passing.**

---

## 🧠 Core Architecture (Phase 8 — Component Testing)

### Single Source of Truth
- **`src/core/IntelligenceDSL.ts`**: Formal specification for cognitive topologies.
- **`src/services/OrchestrationService.ts`**: The execution engine that mounts and runs DSL topologies.
- **`src/core/events.ts`**: The global `EventBus` singleton for all inter-service communication.

### Decision-Centric Model (DCM)
- The atom of intelligence is the **Decision** (`src/services/CognitiveService.ts`).
- Every meaningful AI action MUST be wrapped in a `CognitiveDecision` object containing: goals, alternatives, reasoning, and confidence.

### Cognitive Layers
1. **Execution:** `src/services/AgentService.ts`, `src/services/ToolService.ts`.
2. **Coordination:** `src/services/OrchestrationService.ts`.
3. **Reasoning:** `src/services/DebateService.ts`.
4. **Observability:** `src/services/TraceService.ts`, `src/services/SnapshotService.ts`.
5. **Interpretation:** `src/services/AdvisorService.ts`, `src/services/MemoryService.ts` (Orama Worker + Transformers.js embeddings).

---

## 📡 EventBus Cheat Sheet (V3 Core)

| Event | Payload | Description |
|-------|---------|-------------|
| `cognitive:step:active` | `{nodeId, traceId, metadata?}` | Orchestrator starts processing a DSL node. |
| `cognitive:step:completed` | `{nodeId, traceId, decision, output, duration}` | Node execution finished. Triggers tracers. |
| `system:topology:mounted` | `any` | A new intelligence graph is loaded. |
| `advisor:suggestion` | `any` | Meta-agent proposes structural changes. |
| `memory:updated` | `any[]` | MemoryMesh updated (new cognitive fragment with optional embedding vector). |
| `policy:violation` | `any` | Guardrail alert (Latency, Privacy, Cost). |
| `debate:argument` | `any` | New perspective added to the Dialectic Arena. |

---

## 🧪 Component Test Coverage — 7/21 panels tested
- `AnalyticsPanel.test.tsx` (9 tests), `ChatPanel.test.tsx` (5), `DashboardPanel.test.tsx` (9)
- `EventsPanel.test.tsx` (4), `HealthPanel.test.tsx` (12), `MemoryPanel.test.tsx` (3), `TracesPanel.test.tsx` (3)
- **Gap**: 14 panels remain (AgentsPanel, HivePanel, KnowledgePanel, ConnectorsPanel, SkillsPanel, RolesPanel, TasksPanel, BuilderPanel, ProviderManager, SettingsPanel, DocumentationPanel, LiveCognition, MissionControl, ChatHistory).
- Patterns: wrap panels in `HiveContext` provider, mock `scrollIntoView` globally, use `@testing-library/react`.

## ⚠️ Critical Rules for AI Agents

### 1. TypeScript Strictness
- **`verbatimModuleSyntax`** is enabled. Use `import type` for all type-only imports.
- Never mix types and values in a single `import` statement.

### 2. Decision Wrapping
- When implementing new agentic logic, ensure the output is captured as a `Decision` with alternative reasoning.
- Use `CognitiveService.recordDecision()` to ensure visibility in the Debugger.

### 3. DSL Compliance
- All multi-agent workflows MUST be defined as an `ISTopology`.
- The Orchestrator is the only service that should manage the execution flow between nodes.

### 4. Observability First
- Emit `cognitive:step` events for every logical stage of reasoning.
- This ensures the **Cognition Replay** and **Decision Graph** function correctly.

### 5. IS-Policies
- Always check `PolicyService.getPolicies()` before performing high-cost or high-latency operations.
- Honor PII redaction rules if the `privacy` policy is active.

---

## 🛠 Tech Stack & Tools
- **Design System:** Custom dark glassmorphism (`src/index.css`).
- **Icons:** `lucide-react`.
- **Animations:** `framer-motion`.
- **Build:** Vite 8.0.10 + TS 6.0.3.
- **Search:** Orama (Web Worker) for full-text BM25; Transformers.js (all-MiniLM-L6-v2) for semantic embeddings.
- **Testing:** Vitest — 32 test files, 192 tests (7 component test files for UI panels).
- **Verification:** `npm run build` is mandatory after any architectural change; `npm run test` before commit.

---
**Maintained by:** Antigravity  
**Status:** Synchronized with v3.7.1 Component Testing  
**Tests:** 32 files, 192 passing (last verified: 2026-05-11)
