# AI Agent Context — SuperAgents OS v4.0.3
> **The Observable and Programmable Intelligence Operating System**

This project is an **autonomous, event-driven, decision-centric multi-agent runtime**. It shifts the focus from simple LLM chat to structured cognitive execution.

**Current testing baseline: tsc --noEmit ✅ | vite build ✅ (4s) | kernel tests ✅**

---

## 🧠 Core Architecture (Phase 8 — Runtime Stability) + Phase 3 UI Enhancements

### Single Source of Truth
- **`src/core/IntelligenceDSL.ts`**: Formal specification for cognitive topologies.
- **`src/services/OrchestrationService.ts`**: The execution engine that mounts and runs DSL topologies.
- **`src/core/events.ts`**: The global `EventBus` singleton for all inter-service communication.

### Decision-Centric Model (DCM)
- The atom of intelligence is the **Decision** (`src/services/CognitiveService.ts`).
- Every meaningful AI action MUST be wrapped in a `CognitiveDecision` object containing: goals, alternatives, reasoning, and confidence.

### Cognitive Layers
1. **Execution:** `src/services/AgentService.ts`, `src/services/ToolService.ts`.
2. **Coordination:** `src/services/OrchestrationService.ts`.
3. **Reasoning:** `src/services/DebateService.ts`.
4. **Observability:** `src/services/TraceService.ts`, `src/services/SnapshotService.ts`.
5. **Interpretation:** `src/services/AdvisorService.ts`, `src/services/MemoryService.ts` (Orama Worker + Transformers.js embeddings).

---

## 📡 EventBus Cheat Sheet (V3 Core)

| Event | Payload | Description |
|-------|---------|-------------|
| `cognitive:step:active` | `{nodeId, traceId, metadata?}` | Orchestrator starts processing a DSL node. |
| `cognitive:step:completed` | `{nodeId, traceId, decision, output, duration}` | Node execution finished. Triggers tracers. |
| `system:topology:mounted` | `any` | A new intelligence graph is loaded. |
| `advisor:suggestion` | `any` | Meta-agent proposes structural changes. |
| `memory:updated` | `any[]` | MemoryMesh updated (new cognitive fragment with optional embedding vector). |
| `policy:violation` | `any` | Guardrail alert (Latency, Privacy, Cost). |
| `debate:argument` | `any` | New perspective added to the Dialectic Arena. |

---

## ✅ Phase 2 Complete — Routing & Priority
- **PriorityQueueDecorator** wired into `AdapterFactory` (after CircuitBreaker, before Retry).
- **Priority propagation** through `ChatService` → `LLMClient` via `apiKey` prefix (`high:`/`low:`).
- **Debate provider cycling** — each participant gets unique provider via `getDebateProviders()`.
- **Provider-aware request splitting** — `ChatService.executeRequest()` auto-routes when `provider` is `'auto'`/`undefined`: extracts prompt text, calls `routerService.getRankedProviders('content', ...)`, selects top-ranked provider. Fixed pre-existing `provider` vs `resolvedProvider` bug in `ChatService.ts`.

## ✅ Phase 3 Complete — UI Enhancements (8 items)
1. **PoolStatusPanel** — Fast/Balanced/Free/Experimental pool grid cards + Providers list view with strategy selector and quota editing.
2. **RoutingIntelligence** — Decision Tree tab with visual flow chart (request → classification → strategy → weights → scoring → selection).
3. **HealthPanel** — Rate Limit Introspection section per-key (quota bars, 429 counts, pressure %).
4. **UsageHeatmap** — Real hourly usage from `KeyService.hourlyUsage` array (was random).
5. **AlertLayer** — Global floating toast overlay subscribed to `NOTIFICATION`, `KEY_QUOTA_EXCEEDED`, `KEY_LATENCY_BURST`, `KEY_HEALTH_FAILED`, `KEY_REPUTATION_DOWN`, `KEY_STATE_CHANGED`; auto-dismiss + collapsible alert list.
6. **EventsTimeline** — Search, time/event grouping, localStorage persistence.
7. **AgentsPanelView** — Cost Per Run (avg/total/P50/P95/P99) + Latency Profile (P50/P90/P95/P99, distribution histogram).
8. **System Overview dashboard** — Enhanced System Health Bar with real-time RPS/latency/reqs/cost panel, Resource Pressure Map with load % bars per provider, new Routing Activity section showing latest router decisions.

### Key Data Changes
- `KeyExtendedStats.hourlyUsage: number[]` — 24-element array tracked in `KeyService.recordUsage()`.
- `PoolStatusPanel` listens to `EVENTS.KEY_UPDATED` for live refresh.

---

## ✅ Phase 4 Complete — AI SRE Agent Suite (items 35-39)
1. **AI SRE Agent** — Полноценный агент с LLM-анализом, автофиксами, мониторингом latency/errors/429/quota/budget ✅
2. **Smart diagnostics** — `getSmartDiagnostic()`: 401/403/429/timeout/quota/model errors с трекингом частоты и эскалацией ✅
3. **What-if analysis** — `getWhatIfAnalysis()`: проекции при добавлении ключей, смене стратегии, экономии бюджета ✅
4. **Auto model downgrade** — ChatService: >75% → -1 уровень, >90% → -2 уровня по downgrade chains в RouterService ✅
5. **Prompt caching** — `trackPromptPattern()` + `getPromptCachingAdvice()` с оценкой экономии ✅

## 🧪 Component Test Coverage — 7/21 panels tested (+ 6 new component tests)
- `AnalyticsPanel.test.tsx` (9), `ChatPanel.test.tsx` (11), `DashboardPanel.test.tsx` (9)
- `EventsPanel.test.tsx` (12), `HealthPanel.test.tsx` (12), `MemoryPanel.test.tsx` (3), `TracesPanel.test.tsx` (12)
- `AgentsPanel.test.tsx` (21), `HivePanel.test.tsx` (15), `KnowledgePanel.test.tsx` (9), `ConnectorsPanel.test.tsx` (23)
- `SkillsPanel.test.tsx` (9), `TasksPanel.test.tsx` (12), `BuilderPanel.test.tsx` (11), `DebatePanel.test.tsx` (25)
- `AquariumPanel.test.tsx` (9), `ToolsPanel.test.tsx` (11), `SettingsPanel.test.tsx` (11), `ChatAdminPanel.test.tsx` (13)
- `PoolStatusPanel.test.tsx`, `RoutingIntelligence.test.tsx`, `AlertLayer.test.tsx` — NEW
- **Total**: 57 test files
- **Gap**: 7 panels remain (RolesPanel, ProviderManager, DocumentationPanel, LiveCognition, MissionControl, ChatHistory, EventsTimeline).
- Patterns: wrap panels in `HiveContext` provider, mock `scrollIntoView` globally, use `@testing-library/react`.

## ⚠️ Critical Rules for AI Agents

### 1. TypeScript Strictness
- **`verbatimModuleSyntax`** is enabled. Use `import type` for all type-only imports.
- Never mix types and values in a single `import` statement.

### 2. Decision Wrapping
- When implementing new agentic logic, ensure the output is captured as a `Decision` with alternative reasoning.
- Use `CognitiveService.recordDecision()` to ensure visibility in the Debugger.

### 3. DSL Compliance
- All multi-agent workflows MUST be defined as an `ISTopology`.
- The Orchestrator is the only service that should manage the execution flow between nodes.

### 4. Observability First
- Emit `cognitive:step` events for every logical stage of reasoning.
- This ensures the **Cognition Replay** and **Decision Graph** function correctly.

### 5. IS-Policies
- Always check `PolicyService.getPolicies()` before performing high-cost or high-latency operations.
- Honor PII redaction rules if the `privacy` policy is active.

---

## 🛠 Tech Stack & Tools
- **Design System:** Custom dark glassmorphism (`src/index.css`).
- **Icons:** `lucide-react`.
- **Animations:** `framer-motion`.
- **Build:** Vite 8.0.10 + TS 6.0.3.
- **Search:** Orama (Web Worker) for full-text BM25; Transformers.js (all-MiniLM-L6-v2) for semantic embeddings.
- **Testing:** Vitest — 57 test files.
- **Verification:** `npm run build` is mandatory after any architectural change; `npm run test` before commit.
---

## 🎯 Remediation Backlog (из audit 2026-05-14)

### P0 — Security & Critical (День 1–2) ✅
| # | Задача | Файлы | Сложность | Статус |
|---|--------|-------|-----------|--------|
| 1 | **Execution perimeter: https-only + domain allowlist** — ToolService.fetchUrl(): запрещён `http:`, добавлен `allowedDomains` per-tool, приватные сети уже были | `ToolService.ts` | 2ч | ✅ https-only, allowlist per-tool, isPrivateIP сохранён |
| 2 | **Убрать публичный getEventStream()** — удалён метод, отдававший весь eventBus | `AdminService.ts` | 1ч | ✅ метод удалён |
| 3 | **Починить event-contract: KEY_UPDATED** — добавлена константа в `EVENTS` | `events.ts` | 30м | ✅ `EVENTS.KEY_UPDATED = 'key:updated'` |
| 4 | **KeyService.getKeys() — защита от мутаций** — возвращать копию массива | `KeyService.ts` | 1ч | ✅ `return [...this.keys]` |
| 5 | **SecurityService: ротация пароля с re-encrypt** — в `changePassword()` добавлен callback `reEncrypt` для перешифровки данных новым ключом | `SecurityService.ts` | 3ч | ✅ reEncrypt callback |
| — | **AdminService.test.ts** — переписан на factory вместо singleton (4/4 ✅) | `AdminService.test.ts` | 1ч | ✅ |

### P1 — Stability & Lifecycle (День 3–4) ✅
| # | Задача | Файлы | Сложность | Статус |
|---|--------|-------|-----------|--------|
| 6 | **Boot flow: рендерить shell до runtime.start()** — main.tsx больше не блокирует рендер | `main.tsx` | 4ч | ✅ рендер до runtime.start() |
| 7 | **runtime.ts: убрать хардкод servicesTotal=17** — динамический подсчёт из bootstrap report | `runtime.ts`, `Bootstrap.ts` | 2ч | ✅ servicesTotal из отчёта, shutdown async |
| 8 | **PolicyService: Immutable sanitization** — не мутирует `data.output` | `PolicyService.ts` | 1ч | ✅ |
| 9 | **RouterService: вынести эвристики в config/policy слой** | `RouterService.ts`, `src/types/routing.ts` | 6ч | ✅ |

### P2 — Performance & Consistency (День 5–7)
| # | Задача | Файлы | Сложность |
|---|--------|-------|-----------|
| 10 | **React.lazy для тяжёлых панелей** — Memory, Health, Hive, Builder, Traces, Mission Control вынести из initial bundle | `App.tsx` | 3ч | ✅ |
| 11 | **Ленивая инициализация embeddings/worker** — Transformers.js/Orama не грузить до первого обращения к MemoryPanel | `MemoryService.ts`, `MemoryPanel.tsx` | 3ч | ✅ |
| 12 | **HealthPanel: убрать layout thrashing** — заменить `getBoundingClientRect()` в rAF на CSS-only анимацию | `HealthPanel.tsx` | 2ч | ✅ |
| 13 | **MemoryPanel: фильтрация по коллекциям** — явный `filter()` по `activeCollection` | `MemoryPanel.tsx` | 1ч | ✅ |
| 14 | **Единый источник версии** — вытащить из `package.json` через `import.meta.env`; убрать хардкод из `AdminService`, `SettingsPanel` | `package.json`, `vite.config.ts`, `AdminService.ts` | 2ч | ✅ |
| 15 | **Proxy-конфиг: один source of truth** — свести README + vite.config.ts к единому env-driven прокси | `vite.config.ts`, `README.md`, `.env.example`, `SandboxService.ts` | 1ч | ✅ |

### 🐛 Pre-existing test failures (не от аудита)
| Тест | Падает | Причина |
|------|--------|---------|
| `DebateService.test.ts` | 8/18 | (известно, требуется отдельная диагностика) |
| `SettingsService.test.ts` | 5/8 | (известно, требуется отдельная диагностика) |
| `AdminService.test.ts` | 3/4 | (известно, требуется отдельная диагностика) |
| `AddKeyModal.test.tsx` | 2/12 | (известно, требуется отдельная диагностика) |
| `KeyService.test.ts` | 1/2 | (известно, требуется отдельная диагностика) |

---
**Maintained by:** Antigravity  
**Status:** v4.0.3 — Kernel Hardening Complete  
**Build:** tsc --noEmit ✅ | vite build ✅ (4s)
**Kernel:** deep immutable state, ring buffer event log (O(1)), composite event keys, init validation, whitelist SLA
