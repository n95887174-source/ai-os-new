# 11_OPPORTUNITIES — `agent-quality` roadmap opportunities

Each item: ID, Description, User value, Technical reuse, Effort, Risk, Dependencies, Existing infra, Why now.

## 5 QUICK WINS (UI/config only, no new runtime semantics)

- **QW1 — Add `quality_assurance` persona variant.** Description: new variant in `persona-selector.ts` (keywords test/cover/verify/edge-case/claim/spec; suitableRoles neutral/con/pro; minRound 1). User value: `agent-quality` finally steers debates as a QA voice. Reuse: `PersonaSelector` (existing). Effort: S (1 file). Risk: low (additive). Deps: none. Infra: `persona-selector.ts`. Why now: fixes 🔴#1 immediately, zero regression.
- **QW2 — Graceful persona fallback.** Description: in `selectVariant`, if `eligible` empty, map unknown role to a neutral default variant instead of `undefined`. User value: all non-pro/con/neutral agents get a persona; `agent-quality` included. Reuse: `persona-selector.ts`. Effort: S. Risk: low. Deps: QW1. Infra: same. Why now: removes silent no-persona defect broadly.
- **QW3 — Fix journal `agentName`.** Description: in `agent-journal-service.ts:135,161` resolve name via `resolveAgentIdentity`. User value: human-readable QA history. Reuse: `agent-identity.ts`. Effort: S. Risk: low. Deps: none. Infra: `agent-journal-service.ts`. Why now: cheap readability fix.
- **QW4 — Honor pinned model.** Description: set topology node `config.model` to `llama-3.1-8b-instant` OR make `resolveAgent` prefer profile model when node=`'auto'`. User value: predictable cheap QA runs, matches profile. Reuse: `agent-service.ts:351-353`. Effort: S. Risk: low (model routing). Deps: none. Infra: topology-defaults / agent-service. Why now: fixes 🔴#2.
- **QW5 — QA specialization chips + "Review with QA" button.** Description: show `specializations` on `AgentCard`; add RoomPanel deep-link pre-targeting `agent-quality`. User value: discoverable QA action. Reuse: `resolveAgent.specializations`, RoomPanel. Effort: S-M. Risk: low. Deps: none. Infra: AgentsPanel + RoomPanel. Why now: surfaces dormant capability.

## 5 MEDIUM (small services/events, reuse existing)

- **M1 — QA verdict display event.** Description: add `debate:quality:claim:checked` / `conversation:quality:gate` (display-only) emitted by `agent-quality` turns; render in `LiveActivityStream`. User value: visible QA pass/fail. Reuse: `event-registry.ts` pattern, `EventBus`. Effort: M. Risk: low (display-only). Deps: QW1. Infra: event-registry + UI. Why now: makes QA observable.
- **M2 — QA findings memory.** Description: persist test plans/verdicts to `service-backed-memory` keyed `agent-quality+artifactRef`. User value: continuity across runs. Reuse: `service-backed-memory.ts`. Effort: M. Risk: low. Deps: none. Infra: memory/* . Why now: fixes 🟠#5.
- **M3 — QA invocation policy + task templates.** Description: seed `phase21-invocation.ts` policy pinning `agent-quality` for QA requests; RoomPanel templates from `specializations`. User value: one-click QA review. Reuse: `phase21-invocation.ts`, RoomPanel. Effort: M. Risk: low. Deps: QW5. Infra: invocation. Why now: operationalizes specialization.
- **M4 — QA lens (`lens:qa`).** Description: add a 16th lens "QA / Test / Coverage" to `lens-library.ts` and wire `lensIds` to `agent-quality`. User value: amplifies QA perspective in synthesis/debate. Reuse: `lens-engine`, `agent-identity.ts`. Effort: M. Risk: low. Deps: none. Infra: lens-library + profile. Why now: closes 🟠#4.
- **M5 — Coverage report turn in Director.** Description: a scenario preset "QA Gate" where `agent-quality` summarizes coverage of scenario objectives. User value: acceptance gating. Reuse: `ConversationDirectorService` + `TurnProposal`. Effort: M. Risk: low. Deps: M2. Infra: director. Why now: turns specialization into a workflow.

## 3 BIG IDEAS (architectural, still reuse-first)

- **B1 — Autonomous QA gate in Debate consensus.** Description: before `DEBATE_CONSENSUS`, run a `agent-quality` QA pass rating evidential support; surface as a consensus prerequisite. User value: rigour guardrail. Reuse: `debate-sync-manager` (already finalizes `qualityCollector`), `DEBATE_CONSENSUS` event. Effort: L. Risk: med (debate flow change). Deps: QW1,M1. Infra: debate-runtime. Why now: unique differentiator; today `QualityImpactCollector` measures technique impact, not claim validity.
- **B2 — Agent QA self-test loop.** Description: `agent-quality` periodically re-runs regression checks on other agents' last outputs (read from memory/journal) and files coverage gaps. User value: continuous quality. Reuse: `AgentJournalService`, `service-backed-memory`, scheduler. Effort: L. Risk: med (loop control). Deps: M2. Infra: journal + memory + scheduler. Why now: leverages existing journal as a regression corpus.
- **B3 — Quality Engineer as a shared "quality gate" node in Builder/Workflow.** Description: add a `quality_gate` node type in `builder-agent-service` that invokes `agent-quality` to validate compiled flows before deploy. User value: deploy safety. Reuse: `builder-agent-service`, `agentService.resolveAgent`. Effort: L. Risk: med. Deps: M4. Infra: builder. Why now: reuses Builder's manifest compile + existing agent resolution.
