# 03 — Panel-by-Panel Usability Findings

> Scope: a first-time-user, usability-only lens. Evidence tags: **VERIFIED** (read code), **INFERRED**, **OPINION**.
> Panels read for this file: ChatPanel, DirectorPanel/RunTab, WorkflowPanel, RoomPanel, ForumPanel, ResearchEnginePanel, SchedulerPanel, DebateArena, DebateLivePanel, AgentsPanel(+View/Container), DashboardPanel(+GetStarted/QuickActionBar), CommandPalette, ComingSoonPanel, ModuleInfo, LensesPanel, MemoryPanel (structure).

## Tier A — Mature, well-built (positive evidence)

| Panel                       | Why it's good (VERIFIED)                                                                                                                                                                                                                                                                | Minor gaps                                                                                                                                               |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **DashboardPanel**          | Rich: header, conditional `GetStartedPanel`, `QuickActionBar`, `CriticalAlertBanner`, `StatsGrid`, `SystemHealthPanel`, `ProviderPressureMap`, `RoutingActivitySection`, `InferenceMeshSection`, `LiveTerminalSection` (live event stream), `ModuleInfo`. Subscribes to real event bus. | `GetStartedPanel` shows **only** when `keys.length===0` (key-focused, not a general "where do I start" guide). No persistent onboarding once keys exist. |
| **ChatPanel**               | Most mature: sessions, fork, edit, system prompt, search, export, split view, technical/standard display, model+key selectors.                                                                                                                                                          | Breadth implies a learning curve; no inline tutorial for first use.                                                                                      |
| **AgentsPanel**             | Very rich: list/grid views, search/filter, `AgentCard`, `AgentDetailPanel`, `AgentStatsDashboard`, `LiveActivityStream`, `EloLeaderboard`, `AgentWizard`, `AgentGroupsSection`, import/export, reset stats.                                                                             | Overwhelming for first-timers; many tabs (Config/Capabilities/Handoffs/History/Observability) with no guided entry point.                                |
| **DirectorPanel / RunTab**  | Full run UI: scenario select, live turn log, progress %, override proposal, checkpoints; bound to `directorController`+`useDirectorStore`. Clear empty state when no scenario.                                                                                                          | "Scenario / TurnProposal / override" vocabulary is expert-only.                                                                                          |
| **WorkflowPanel**           | **VERIFIED** shows real run history (`runs` filtered by workflowId, progress bar + per-step status). Corrects earlier inference that it lacked history.                                                                                                                                 | Naming "Workflow" vs "Builder" vs "Director scenario" overlaps conceptually.                                                                             |
| **CommandPalette**          | **VERIFIED** excellent: Cmd/Ctrl+K, fuzzy match, recent items, grouped by section, keyboard nav (↑↓/↵/ESC), clear. A genuine usability win.                                                                                                                                             | Not advertised in-UI; first-time users won't discover it.                                                                                                |
| **LensesPanel** (cognitive) | **VERIFIED** real, functional: list/toggle lenses, stack visualizer, editor modal, suggestions. Proper loading/empty handling.                                                                                                                                                          | "Lens stack / validation" metaphor undocumented for newcomers.                                                                                           |
| **MemoryPanel**             | **VERIFIED** has dedicated `MemoryEmptyState`, `MemoryErrorAlert`, `MemoryHeader`, `SearchBar`, tabs. Good empty/error pattern other panels should copy.                                                                                                                                | —                                                                                                                                                        |

## Tier B — Functional but with real usability gaps

| Panel                   | Findings (VERIFIED unless noted)                                                                                                                                                                                                                                                                                                                         |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **RoomPanel**           | (see 01/05) "Where" (room/forum-topic/conversation) and "Mode" (chat/debate/director-scenario) ambiguous to new users; the word "invocation" never appears in UI though it's the core concept. Feed is global/unscoped. "Clear" clears local view only (history persists). Rejection reasons shown as raw technical text ("no matching enabled policy"). |
| **ForumPanel**          | (see 05) Backend rich (`votePost`/`pinTopic`/`moderatePost`/`getConsensus`) but UI shows only consensus status text — **no** vote/pin/moderate UI, no escalation action. Author hardcoded "Вы" (Russian "You").                                                                                                                                          |
| **ResearchEnginePanel** | Backend computes 7 phases (citation graph, knowledge graph, systematic review, fact-check, anomaly detection, peer-review, discovery, citation export) but UI exposes only session loops + partial citation export (bibtex/apa/mla/chicago). 6–7 phases are **dark** (no UI entry).                                                                      |
| **SchedulerPanel**      | **VERIFIED** static mock: hardcodes `SCHEDULES`; toggles only flip a settings flag via `setSetting`/`getAllSettings`; never touches `SchedulerService`. Looks functional but does nothing.                                                                                                                                                               |
| **DebateArena**         | **VERIFIED** clean classic/runtime toggle. But the 30 debate sub-panels under it are stubs (see below).                                                                                                                                                                                                                                                  |
| **DebateLivePanel**     | Circular arena; when no session, empty arena + session selector, no first-time guidance/empty-state copy.                                                                                                                                                                                                                                                |

## Tier C — Phantom / stub panels (SYSTEMIC, P0)

- **~30 debate sub-panels** (steelman, bayesian-judge, blind-eval, credibility, calibration, consistency, frame-tracker, stance-drift, insight-bus, entanglement, anchoring, meta-agent, outcome-forecaster, concept-blender, belief-mining, minimax-planner, expert-witness, rhetoric, bias-profiler, incentive-detector, stakeholder, scratchpad, persona-mixer, bop-tracker, got-deliberation, similarity, drift-detector, shadow-opponent, adversarial-source, vuln-targeting, justification, logical-form) all map to `ComingSoonPanel` in `route-imports.ts:362-393`.
- **`ComingSoonPanel`** renders "This panel is coming soon. It will display data from `<service>`." but:
  - `ModuleInfo moduleKey={route}` is cast `as unknown as ModuleKey`. `ModuleInfo`'s `MODULE_INFO_KEY`/`MODULE_ICONS`/`MODULE_NAV_KEY` maps **do not contain** these stub keys → `t(undefined)` and `MODULE_ICONS[undefined]` → the module-info block renders **empty/garbage** for stubs. So stubs look _worse_ than a real ComingSoon because the helpful description silently disappears.
  - The **Sidebar never flags stubs** — it renders them identically to real panels (only `featureFlag` is read, not `experimental`). Users cannot tell real from placeholder.
- **`builder` nav id duplicated** in Debates (`route-registry-core.ts:146`) and Knowledge (`route-registry-content.ts:106`) → both point to `CognitiveBuilder`.

## Cross-panel consistency (see 09 for full)

- Empty states: MemoryPanel has `MemoryEmptyState`; many panels (stubs, Forum, Research) do not.
- Naming collisions: Workflow / Builder / Director scenario / Room "Mode: director-scenario" all describe orchestration — a first-time user cannot tell them apart.
- Author/identity: Forum hardcodes "Вы"; elsewhere agent identity is configurable (`AgentIdentityEditor`).

## Deliverables index

- [00_USABILITY_MASTER.md](./00_USABILITY_MASTER.md) · [01_FIRST_TIME_USER.md](./01_FIRST_TIME_USER.md) · [02_NAVIGATION.md](./02_NAVIGATION.md) · [04_USER_JOURNEYS.md](./04_USER_JOURNEYS.md) · [05_AGENT_UX.md](./05_AGENT_UX.md) · [06_LIVE_REALTIME_UX.md](./06_LIVE_REALTIME_UX.md) · [07_FORMS_AND_CONTROLS.md](./07_FORMS_AND_CONTROLS.md) · [08_INFORMATION_ARCHITECTURE.md](./08_INFORMATION_ARCHITECTURE.md) · [09_CONSISTENCY.md](./09_CONSISTENCY.md) · [10_ERROR_UX.md](./10_ERROR_UX.md) · [11_DISCOVERABILITY.md](./11_DISCOVERABILITY.md) · [12_USABILITY_SCORECARD.md](./12_USABILITY_SCORECARD.md) · [13_UX_RECOMMENDATIONS.md](./13_UX_RECOMMENDATIONS.md) · [14_TOP_50_IMPROVEMENTS.md](./14_TOP_50_IMPROVEMENTS.md)
