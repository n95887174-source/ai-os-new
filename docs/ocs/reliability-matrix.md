# Security & Reliability Hardening Matrix

> Системный аудит классов отказов, а не файлов.
> Дата: 2026-07-25 | Версия: v4.5.0 → v4.6.0

---

## Legend

| Symbol | Meaning                                                    |
| ------ | ---------------------------------------------------------- |
| ✅     | Fully covered — pattern exists, all known instances use it |
| ⚠️     | Partial — pattern exists but has gaps                      |
| ❌     | Missing — no protection at all                             |
| 🔍     | Detection exists (lint/type/runtime check)                 |
| 🚫     | No detection                                               |

---

## Full Matrix

| #   | Failure Class                  | Detection                                                  | Protection | Coverage | Key Gaps                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | ------------------------------ | ---------------------------------------------------------- | ---------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Race conditions**            | 🔍 `_lock`/`_syncing` patterns in ~10 services             | ⚠️         | ~72%     | CRITICAL-1: `LLMHttpClient._inflight` static Map no mutex. CRITICAL-2: `CircuitBreaker.states` TOCTOU. CRITICAL-3: `RateLimitDecorator` tokens race. HIGH-1: `CacheService` set/clear/invalidate race. HIGH-5: `PriorityQueueDecorator` queues race.                                                                                                                                                                                                                                                                     |
| 2   | **Idempotency**                | 🚫                                                         | ⚠️         | ~41%     | `emitOnce()` exists but **never called** (all 433+ emits unprotected). PricingService `dedupKey` unimplemented. No HTTP idempotency keys on any LLM adapter. EventRecorder records duplicates.                                                                                                                                                                                                                                                                                                                           |
| 3   | **Dual-write**                 | 🚫                                                         | ⚠️         | ~40%     | `persistThenEmit` utility + `Outbox` class created (Session 35). Fixed: `key-service.ts handleProviderError` (emit→saveKeys), `key-state-store.ts update/remove` (emit→persist), `role-service.ts deleteRole` (emit→persist). `config-service.ts` (9 methods), `tool-executor.ts` (4 methods) still correct. Chat store still 13+ Zustand-first. ITransaction exists but only used in `kernel.ts`. New Outbox utility available for future fixes.                                                                        |
| 4   | **Event loss**                 | 🔍 WAL crash recovery in EventRecorder + DLQ               | ⚠️         | ~50%     | Durable event log exists but APPEND-ONLY — no state rebuild. Dead letter queue created (Session 33) + wired to `notification-webhook-service` + `debate-llm-caller` (4 retry exhaustion paths, Session 36). DLQ registered in DI container. `QUEUE_TASK_FAILED` event defined but never emitted. `emit()` returns void, callers cannot know delivery status.                                                                                                                                                             |
| 5   | **Event duplication**          | 🔍 `emitOnce()` infrastructure                             | ⚠️         | ~30%     | 10 STREAM_END subscribers — only BudgetService has dedup. Cross-tab sync creates duplicate processing (local + BroadcastChannel). Self-subscription in `debate-sync-manager.ts` (subscribes to own event).                                                                                                                                                                                                                                                                                                               |
| 6   | **Partial failure / rollback** | 🚫                                                         | ❌         | ~10%     | `ITransaction` with compensation model exists but only used in `kernel.ts`. No saga pattern implementation. No rollback mechanism for any service outside kernel. MemoryEngine has 3 independent stores with no rollback. Session 35: 3 emit-before-persist sites fixed — failed persist no longer emits stale events (key-service, key-state-store, role-service).                                                                                                                                                      |
| 7   | **Crash consistency**          | 🔍 EventRecorder WAL + gen counter                         | ⚠️         | ~25%     | WAL recovers unpersisted events after crash. But most services store directly to Dexie without WAL — a crash mid-write can leave partial data. Dexie's IndexedDB is ACID per transaction but no coordination across tables.                                                                                                                                                                                                                                                                                              |
| 8   | **Stale state / versioning**   | 🔍 DexieDebateStore optimistic concurrency                 | ⚠️         | ~20%     | Only `dexie-storage.ts` debate tables have version conflict detection. Chat store uses `updatedAt` comparison in liveQuery merge. No CAS (compare-and-swap) pattern elsewhere.                                                                                                                                                                                                                                                                                                                                           |
| 9   | **Lost updates**               | 🚫                                                         | ⚠️         | ~25%     | Session 37: 30+ fire-and-forget persist calls converted to `await` across 8 services — failed persists now propagate errors instead of silent data loss. Remaining: Zustand `set()` succeeds before Dexie write (chat store), trace-service.ts event handlers (EventBus onSafe doesn't support async), MemoryEngine worker fire-and-forget (separate concern).                                                                                                                                                           |
| 10  | **Ordering bugs**              | 🚫                                                         | ❌         | ~20%     | `EventRecorder` assigns sequence numbers. No causal ordering guarantees. Chat store `_sendQueue` provides FIFO per session. No global ordering mechanism. EventBus handlers fire synchronously in registration order but deferred events lose ordering. Session 35: 3 persist-before-emit reorderings fixed — events now fire after data is durable.                                                                                                                                                                     |
| 11  | **Retry storms**               | 🔍 All backoff implementations have jitter                 | ⚠️         | ~70%     | `RetryDecorator`: exponential + jitter ✅. `batch-processor-service.ts`: jitter added (Session 32). `debate-llm-caller.ts backoffWait()`: jitter added (Session 32). `debate-persistence-manager.ts`: jitter added (Session 39). All known retry paths now have jitter. No remaining spin-loops.                                                                                                                                                                                                                         |
| 12  | **Infinite retries / DLQ**     | 🔍                                                         | ⚠️         | ~30%     | Dead letter queue service created + registered in DI (Session 33/36). Wired to `notification-webhook-service.ts` (2 paths) and `debate-llm-caller.ts` (4 retry exhaustion paths). Most services still silently give up. No poison message handling beyond DLQ storage — retry/remove is manual.                                                                                                                                                                                                                          |
| 13  | **Fire-and-forget**            | 🔍                                                         | ⚠️         | ~55%     | Session 37: Fixed 30+ fire-and-forget persist calls across 8 files (role-service, task-handoff, tool-executor, skill-service, metrics-service, policy-service, prompt-security-service). Converted `.then()` patterns to `await` in tool-executor (addTool, removeTool, toggleTool). Remaining: trace-service.ts event handlers (6 sites — EventBus onSafe doesn't support async), agent-health-monitor.ts (2 sites — can add .catch()).                                                                                 |
| 14  | **Resource leaks**             | 🔍                                                         | ✅         | ~97%     | 59+ AbortController instances all properly cleaned. Workers terminated in destroy. `BudgetService.destroy()` cleaned (Session 32 — `_saveTimer`, `sentAlerts`, `agentBudgets`, `agentSpend`, `_costDedupSet`, `alertsHistory`, `budgetInfoCache`, `_monthFiltered`). All known timer/collection leaks resolved.                                                                                                                                                                                                          |
| 15  | **Memory leaks**               | 🔍                                                         | ✅         | ~97%     | C1/C7 module-level maps fixed in Session 6. `regexCache` LRU fixed in Session 25. `dailyStats` pruning fixed in Session 26. `cache-service.ts` maxEntries=500 with LRU. `BudgetService.destroy()` cleaned in Session 32. All known collection/timer leaks resolved.                                                                                                                                                                                                                                                      |
| 16  | **Backpressure**               | 🚫                                                         | ⚠️         | ~40%     | EventBus recursion guard (depth 32 → defer, 1000 → drop). No bounded queue on EventBus subscriptions. `PriorityQueueDecorator` has queues but no backpressure to callers. LLMHttpClient has concurrency limit (`SETTINGS_MAX_CONCURRENT`). No end-to-end backpressure from LLM response to UI.                                                                                                                                                                                                                           |
| 17  | **Concurrency overload**       | 🔍                                                         | ⚠️         | ~60%     | `BatchProcessorService` CONCURRENCY=5 ✅. LLMHttpClient `_inflight` bounded by timeouts ✅. RateLimitDecorator token bucket (weak due to TOCTOU race). CircuitBreaker half-open guard (weak due to TOCTOU). No semaphore pattern in most services.                                                                                                                                                                                                                                                                       |
| 18  | **Cache inconsistency**        | 🚫                                                         | ⚠️         | ~45%     | `pendingSet` pattern added (Session 34): explicit `set()`/`clear()`/`invalidate()` mark in-flight keys as stale, preventing `getOrFetch` from overwriting with stale data. `clear()` also clears `inFlight`. Still: no write-through cache, no cache coherency protocol across tabs.                                                                                                                                                                                                                                     |
| 19  | **Config drift**               | 🚫                                                         | ⚠️         | ~60%     | Module-level CONFIG captures → getter functions (Session 20 fix for 8 files). But: no schema validation for overlay shapes. `config-mutations.ts setConfig()` accepts any key. Config import (`DexieConfigStore`) only validates `id` field. No per-key schema.                                                                                                                                                                                                                                                          |
| 20  | **Schema drift**               | 🔍                                                         | ⚠️         | ~80%     | Dexie migration system (v5→v12) with upgrade handlers. Zod write hooks on **14 tables** (Session 38: added `debateTimeline`, `debateOverrides`, `sessionLinks`, `eventLog` with new Zod schemas). Remaining: `validateMigrations()` only compares index strings — doesn't verify data integrity.                                                                                                                                                                                                                         |
| 21  | **Corrupt persistence**        | 🔍 EventRecorder checksums + Zod hooks + import validation | ⚠️         | ~60%     | EventRecorder SHA-256 checksums. Zod write hooks on 14/16 Dexie tables (Session 38). `dexie-storage.ts` all 7 `importAll()` methods upgraded from basic `validateArrayItems` to full Zod schema validation (Session 39). `database-service.ts` `importFromJson()` now pre-validates all 16 tables via Zod before `bulkPut` (Session 39). Still missing checksums on localStorage data. No periodic integrity scan.                                                                                                       |
| 22  | **Partial initialization**     | 🚫                                                         | ⚠️         | ~50%     | `_initialized` flags in many services. `LifecycleManager.initAll()` sequential with threshold. No readiness probe. Some services don't check `_initialized` before accepting calls. `RuntimeManager` has loading/ready/degraded states.                                                                                                                                                                                                                                                                                  |
| 23  | **Shutdown races**             | 🚫                                                         | ⚠️         | ~50%     | `DebateEngine._trackOp` + `destroy()` awaits pending ops (Session 6 C11 fix). `_destroyed` flag pattern in ~10 services. BudgetService `_saveTimer` can fire after destroy. EventBus `clearAllSubscriptions()` cleans up. No graceful shutdown protocol across services.                                                                                                                                                                                                                                                 |
| 24  | **HMR issues**                 | 🚫                                                         | ⚠️         | ~40%     | `main.tsx` correctly removes `unhandledrejection` handler on HMR. `RuntimeManager` constructor adds handler but **never removes** (duplicate on HMR). Zustand stores use `import.meta.hot.dispose()` for liveQuery cleanup. EventBus `clearAllSubscriptions()` on HMR. Some module-level Maps can leak on HMR (Session 6 C1 fixed this for debate modules).                                                                                                                                                              |
| 25  | **Cross-tab races**            | 🔍                                                         | ⚠️         | ~60%     | `cross-tab-state.ts` has per-message type handling. `debate-update` uses seq-based dedup (line 364). `invalidate-circuit-breaker`/`-rate-limit`/`-error` do NOT re-emit events, only update local state. `key-update`/`settings-update`/`kernel-state-update`/`chat-session-update` re-emit on receiving tab, but subscribers are UI panels only (idempotent re-render). No distributed lock, but only low-risk duplicate processing remains. **Reclassified from 25% → 60% after false-positive analysis (Session 34)** |
| 26  | **Worker races**               | 🚫                                                         | ⚠️         | ~50%     | `memory-engine.ts` worker: postMessage + onmessage with content-based routing. No message correlation IDs. No ordering guarantees. `sandbox-service.ts` worker: same pattern.                                                                                                                                                                                                                                                                                                                                            |
| 27  | **Network failures**           | 🔍                                                         | ✅         | ~80%     | LLMHttpClient timeout + abort + retry. `RetryDecorator` exponential backoff + jitter. CircuitBreaker for 429. Webhook retry. ReconnectionService with backoff. Missing: MCP resource listing no retry. Key health check no retry.                                                                                                                                                                                                                                                                                        |
| 28  | **Provider failures**          | 🔍                                                         | ⚠️         | ~65%     | CircuitBreaker per provider. Health scoring. Fallback chains (RaceExecutor). Preflight health check before debate. Missing: some adapters bypass LLMHttpClient (raw fetch). No provider health SLA enforcement.                                                                                                                                                                                                                                                                                                          |
| 29  | **API rate limits**            | 🔍                                                         | ⚠️         | ~55%     | RateLimitDecorator (token bucket, TOCTOU race). CircuitBreaker handles 429 → transition. KeyService marks rate_limited (Session 14 fix). BudgetService per-provider budgets. Missing: no quota-aware scheduling. No rate limit prediction. No coordinated backoff across concurrent sessions.                                                                                                                                                                                                                            |
| 30  | **Budget overruns**            | 🔍                                                         | ⚠️         | ~60%     | BudgetService monthly/per-provider/per-agent limits. Cost dedup via `_costDedupSet`. Alerts on threshold breach. Missing: no hard stop (soft limit only). No reservation system. Debounced save makes budget tracking eventually consistent at best.                                                                                                                                                                                                                                                                     |
| 31  | **State-machine violations**   | 🔍                                                         | ⚠️         | ~65%     | `DebateSession.transition()` validates transitions (`_transitioning` guard, phase map). `_transitioning` prevents re-entrancy but not async race (returns `false` silently). `SessionManager` resume guard. Missing: formal state machine definition. No state machine visualization/monitoring.                                                                                                                                                                                                                         |
| 32  | **Invalid events**             | 🔍                                                         | ✅         | ~95%     | EventBus validates every event against Zod schemas. Strict mode blocks invalid payloads. Per-handler try/catch prevents one bad handler from crashing others. `EventRecorder` logs all events with checksum. Missing: no event schema versioning for forward compatibility.                                                                                                                                                                                                                                              |
| 33  | **Event replay bugs**          | 🚫                                                         | ❌         | ~15%     | `TemporalReplayService` applies events to provider metrics — **not idempotent**, replaying applies state changes twice. `SnapshotService.restore()` overwrites state (OK for snapshots, but can lose concurrent changes). EventRecorder has no replay-nonce protection.                                                                                                                                                                                                                                                  |
| 34  | **Non-determinism**            | 🚫                                                         | ❌         | ~5%      | No controlled randomness seeds. `Math.random()` used throughout for jitter (good), retry backoff (good), and some algorithm decisions (potentially bad for reproducibility). No seedable RNG in debate/consensus algorithms.                                                                                                                                                                                                                                                                                             |
| 35  | **Lost observability**         | 🔍                                                         | ⚠️         | ~50%     | EventRecorder captures most events (excludes high-frequency cognitive traces). No event delivery SLA monitoring. No trace of events that were dropped (recursion guard, no subscribers). `QUEUE_TASK_FAILED` defined but never emitted. Durable audit trail exists but is append-only.                                                                                                                                                                                                                                   |
| 36  | **Silent errors**              | 🔍                                                         | ⚠️         | ~45%     | `console.error` in EventBus handler catch blocks. `.catch(() => {})` in chat executor (some fixed in Sessions 23-24 to emit NOTIFICATION). `beforeunload` handlers fire-and-forget saves silently. Webhook retry exhaustion logs but no user-facing notification.                                                                                                                                                                                                                                                        |
| 37  | **Unhandled promises**         | 🔍                                                         | ⚠️         | ~70%     | `window.addEventListener('unhandledrejection')` with `preventDefault()` in `runtime.ts` and `main.tsx`. 50+ fire-and-forget `.catch()` chains. Some `.catch(console.error)` patterns. Missing: `DOMException` filtering (abort errors are normal). No structured error propagation to user.                                                                                                                                                                                                                              |
| 38  | **Security boundary failures** | 🔍                                                         | ⚠️         | ~60%     | PermissionGate removed DEV bypass (Session 7). ConfigService requires `requireLevel('L2')` for mutations (Session 19). `adminToken` non-enumerable + UUID default (Session 8/11). Vault system re-enabled (Session 15). Missing: no token rotation. No audit log for auth failures. No capability-based security model.                                                                                                                                                                                                  |
| 39  | **Data corruption**            | 🔍                                                         | ⚠️         | ~30%     | EventRecorder checksums + WAL recovery. Dexie versioning + migration. Debate bootstrap type guard (Session 30). Missing: no table-level checksums. No referential integrity. No periodic integrity scan. `importFromJson()` 16 tables unvalidated. `validateMigrations()` doesn't verify data.                                                                                                                                                                                                                           |

---

## Coverage Summary

| Range          | Count | Classes                                                                                                                                                                                                                                                                                                                                                                     |
| -------------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ✅ **80-100%** | 5     | Network failures (80%), Invalid events (95%), Resource leaks (87%), Memory leaks (87%), Unhandled promises (70%→lower bound)                                                                                                                                                                                                                                                |
| ⚠️ **50-79%**  | 14    | Race conditions (72%), Retry storms (65%), Concurrency overload (60%), Config drift (60%), Schema drift (60%), Provider failures (65%), Budget overruns (60%), State-machine violations (65%), State consistency (55%), API rate limits (55%), Security boundaries (60%), Partial initialization (50%), Shutdown races (50%), Worker races (50%)                            |
| ⚠️ **50-79%**  | 15    | Race conditions (72%), Retry storms (70%), Corrupt persistence (60%), Concurrency overload (60%), Config drift (60%), Schema drift (60%), Provider failures (65%), Budget overruns (60%), State-machine violations (65%), State consistency (55%), API rate limits (55%), Security boundaries (60%), Partial initialization (50%), Shutdown races (50%), Worker races (50%) |
| ⚠️ **20-49%**  | 11    | Idempotency (41%), Event loss (35%), Event duplication (30%), Crash consistency (25%), Stale state (20%), Backpressure (40%), HMR issues (40%), Silent errors (45%), Cross-tab races (25%), Fire-and-forget (30%), Lost observability (50%), Data corruption (30%)                                                                                                          |
| ❌ **0-19%**   | 8     | Dual-write (18%), Partial failure/rollback (5%), Lost updates (10%), Ordering bugs (15%), Infinite retries/DLQ (5%), Cache inconsistency (15%), Corrupt persistence (10%), Event replay bugs (15%), Non-determinism (5%)                                                                                                                                                    |

---

## Critical Next Actions (by impact × effort)

### Quick wins (high impact, low effort)

1. **🔧 Fix BudgetService.destroy()** — `_saveTimer` + 4 collections not cleaned
2. **🔧 Wire debate Zod hooks** — `debateSessions`, `debateVerdicts` schemas defined but unused
3. **🔧 Add jitter to `batch-processor-service.ts` retry** — prevents thundering herd
4. **🔧 Add jitter to `debate-llm-caller.ts backoffWait()`** — prevents synchronized retry waves
5. **🔧 Add delay to `debate-persistence-manager.ts` retry** — spin-loop on version conflict

### Architectural improvements (high impact, medium effort)

6. **🏗️ Use `emitOnce()` at critical emit sites** — idempotency for STREAM_END, NOTIFICATION, DEBATE_UPDATED
7. **🏗️ Fix RateLimitDecorator TOCTOU** — make `checkRate()` + decrement atomic
8. **🏗️ Fix LLMHttpClient.\_inflight static Map race** — instance-level tracking
9. **🏗️ Add dead letter queue** — persistently store failed events
10. **🏗️ Wire ITransaction into key services** — start with memory-engine.ts triple-write

### Fundamental rewrites (high impact, high effort)

11. **🔨 Outbox pattern for chat store** — transactional boundary between Zustand and Dexie
12. **🔨 Checksums on all Dexie tables** — detect silent corruption
13. **🔨 Formal state machine for debate sessions** — async-safe transitions
14. **🔨 Cross-tab distributed lock** — prevent concurrent debate/chat mutations

---

## Audit Methodology

Each failure class was audited using:

1. **Automated grep across `src/kernel/services/`**, `src/stores/`, `src/llm/`, `src/components/`
2. **Code review of 60+ files** across all failure classes
3. **Historical context** from Sessions 1-31 (pre-existing fixes tracked)
4. **Pattern analysis** — existing protections (locks, dedup sets, validators) vs gaps

### Detection types:

- **🔍 Static** — TypeScript type checks, ESLint rules, runtime validators
- **🔍 Runtime** — try/catch, guard clauses, state checks, WAL recovery
- **🔍 Architectural** — DI lifecycle, contract interfaces, event schemas
- **🚫 None** — no mechanism to detect this failure class

---

## Appendix: Per-Service Risk Heatmap

| Service                    | File                            | Failure Classes at Risk                   | Overall Risk          |
| -------------------------- | ------------------------------- | ----------------------------------------- | --------------------- |
| `BudgetService`            | `budget-service.ts`             | Memory leak, fire-and-forget, retry storm | 🔴 HIGH               |
| `RateLimitDecorator`       | `rate-limit-decorator.ts`       | Race condition, rate limit bypass         | 🔴 HIGH               |
| `LLMHttpClient`            | `llm-http-client.ts`            | Race condition, resource leak             | 🔴 HIGH               |
| `CircuitBreakerDecorator`  | `circuit-breaker.ts`            | Race condition, protection bypass         | 🔴 HIGH               |
| `ChatStore`                | `stores/chat/store.ts`          | Dual-write, lost updates, fire-and-forget | 🔴 HIGH               |
| `MemoryEngine`             | `memory-engine.ts`              | Triple-write inconsistency, dual-write    | 🟠 MEDIUM             |
| `DebatePersistenceManager` | `debate-persistence-manager.ts` | Retry storm, spin-loop                    | 🟠 MEDIUM             |
| `BatchProcessorService`    | `batch-processor-service.ts`    | Retry storm, jitter absence               | 🟠 MEDIUM             |
| `CacheService`             | `cache-service.ts`              | Race condition, cache inconsistency       | 🟠 MEDIUM             |
| `PriorityQueueDecorator`   | `priority-queue.ts`             | Race condition, ordering                  | 🟠 MEDIUM             |
| `EventRecorder`            | `event-recorder.ts`             | Event duplication, no replay              | 🟢 LOW                |
| `ConfigService`            | `config-service.ts`             | Config drift, no schema                   | 🟢 LOW                |
| `DebateSyncManager`        | `debate-sync-manager.ts`        | Race with finalize                        | 🟢 LOW (mostly fixed) |
| `DebateEngine`             | `debate-engine.ts`              | Shutdown race, preflight race             | 🟢 LOW (mostly fixed) |
